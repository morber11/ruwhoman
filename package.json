{
    "name": "ruwhoman",
    "private": true,
    "packageManager": "npm@11.3.0",
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "dev": "turbo dev --env-mode=loose",
        "build": "turbo build --env-mode=loose",
        "test": "turbo test --env-mode=loose",
        "lint": "turbo lint --env-mode=loose",
        "check-types": "turbo check-types --env-mode=loose"
    },
    "devDependencies": {
        "turbo": "^2.7.0"
    }
}