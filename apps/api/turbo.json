{
    "extends": [
        "//"
    ],
    "tasks": {
        "build": {
            "env": [
                "DATABASE_URL",
                "FRONTEND_URL",
                "PORT",
                "RECAPTCHA_SECRET_KEY"
            ],
            "inputs": [
                "$TURBO_DEFAULT$",
                ".env"
            ]
        },
        "dev": {
            "inputs": [
                "$TURBO_DEFAULT$",
                ".env"
            ]
        },
        "test": {
            "env": [
                "DATABASE_URL",
                "FRONTEND_URL",
                "RECAPTCHA_SECRET_KEY"
            ]
        }
    }
}